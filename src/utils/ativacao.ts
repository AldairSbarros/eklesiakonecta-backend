// import crypto from 'crypto';

// export function gerarChaveAtivacao(dados: string) {
//   const segredo = process.env.SEGREDO_ATIVACAO || 'segredo-padrao';
//   return crypto.createHash('sha256').update(dados + segredo).digest('hex');
// }